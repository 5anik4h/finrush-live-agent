# Finrush — Master Environment Template
# See backend/.env.example and frontend/.env.example for full variable list
#
# Quick Setup:
#   cp backend/.env.example backend/.env     → fill with your Supabase/Gemini keys
#   cp frontend/.env.example frontend/.env   → fill with your Supabase URL/key
#
# NEVER commit .env files to git (they are gitignored)
